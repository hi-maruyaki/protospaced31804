<main class="main">
  <div class="inner">
        <% if user_signed_in? %>
          <div class="greeting">
          <%= link_to "#{current_user.name }さん、こんにちは!", root_path, class: :greeting__link %>
          </div>       
        <% end %>
     


  </div>

      <div class="card__wrapper">
    
      <%= render partial: 'prototype', collection: @prototypes %>

    </div>
</main>

